# Welcome to the SFS Mod DataCenter
The SFS mod datacenter was created to feed SCAN with data (SCAN is hungry <) )

## How does the JSON mod work?
How does the JSON module work?

each field ```"xxxx": xxx,``` or ```"xxxx": "xxx",```